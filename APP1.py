import streamlit as st

# ページ設定
st.set_page_config(page_title="C言語 はじめの一歩", page_icon="💻", layout="centered")

st.title("💻 C言語 はじめの一歩")
st.write("プログラミングがはじめての人でも大丈夫。中学生にもわかるように、C言語の基本をやさしく解説するアプリです。左のメニューから学びたい項目を選んでください。")

# サイドバーのメニュー構成
st.sidebar.header("📚 学習メニュー")
menu = st.sidebar.radio(
    "項目を選択:",
    (
        "1. はじめに",
        "2. C言語ってなに？",
        "3. さいしょのプログラム",
        "4. 変数(へんすう)を使おう",
        "5. 計算してみよう",
        "6. もし〜なら（if文）",
        "7. くり返し（for文）",
        "8. 確認クイズ",
        "9. 用語集",
    ),
)

# ================= 1. はじめに =================
if menu == "1. はじめに":
    st.header("1. はじめに")

    st.write("#### プログラミングって、何をすること？")
    st.write(
        "プログラミングとは、コンピュータに「やってほしいこと」を、コンピュータがわかる言葉"
        "（プログラミング言語）で指示することです。人にお願いごとをするときと同じように、"
        "順番や言い方が大事になります。"
    )

    st.write("#### どうしてC言語を学ぶの？")
    st.write(
        "C言語は、今使われているたくさんのプログラミング言語のもとになった、いわば「言語のご先祖さま」です。"
        "仕組みを理解しておくと、あとで他の言語を学ぶときにも役立ちます。"
    )

    st.info(
        "💡 実際にパソコンにC言語をインストールしなくても、このアプリだけで学べます。"
        "自分でプログラムを動かしてみたくなったら、「paiza.io」のようなブラウザだけで使える"
        "C言語の実行サービスがおすすめです。"
    )

# ================= 2. C言語ってなに？ =================
elif menu == "2. C言語ってなに？":
    st.header("2. C言語ってなに？")
    st.write(
        "C言語は、1970年代に生まれた、とても歴史のあるプログラミング言語です。"
        "OSや組み込みシステム（家電・車など）のように、**処理の速さ**や**細かい制御**が必要な場面で、"
        "今も現役で活躍しています。"
    )

    st.write("#### 身近な例")
    st.markdown("""
- 🎮 ゲーム機のシステム
- 🚗 車のエンジン制御
- 📱 スマートフォンのOSの一部
- 🖥️ パソコンのOS（Windows・Linuxなど）
""")

    st.info(
        "💡 **たとえ話**\n\n"
        "プログラミング言語には、「人にとってわかりやすい言語」と「コンピュータにとって処理が速い言語」があります。"
        "C言語は後者の代表で、実はPythonのような新しい言語の多くも、内部はC言語で作られています。"
    )

# ================= 3. さいしょのプログラム =================
elif menu == "3. さいしょのプログラム":
    st.header("3. さいしょのプログラム（Hello World）")
    st.write("プログラミングの世界では、いちばんはじめに「Hello, World!」という文字を画面に表示するのが伝統です。さっそく見てみましょう。")

    c_code = """#include <stdio.h>

int main(void) {
    printf("Hello, World!\\n");
    return 0;
}"""
    st.code(c_code, language="c")

    st.write("実行すると、画面には次のように表示されます。")
    st.code("Hello, World!", language=None)

    with st.expander("🔍 1行ずつ解説を見る（クリックで開く）"):
        st.markdown("""
- **`#include <stdio.h>`**：画面への文字の表示など、基本的な機能を使うための準備の合図です。
- **`int main(void)`**：C言語のプログラムは、必ずこの`main`という場所から実行がスタートします。
- **`{ }`（波かっこ）**：ここからここまでが「ひとまとまりの処理」という目印です。
- **`printf("...")`**：かっこの中の文字を画面に表示する命令です。
- **`\\n`**：「ここで改行する」という意味の特別な記号です。
- **`return 0;`**：「プログラムは正常に終わりました」とコンピュータに伝える合図です。
- **`;`（セミコロン）**：C言語では、1つの命令の終わりに必ず`;`をつけます。忘れるとエラーになるので注意しましょう。
""")

# ================= 4. 変数(へんすう)を使おう =================
elif menu == "4. 変数(へんすう)を使おう":
    st.header("4. 変数(へんすう)を使おう")
    st.write("**変数**とは、数字や文字を入れておく「名前のついた箱」のようなものです。")
    st.write("C言語では箱を使う前に、「この箱には何を入れるか」を先に決めておく必要があります。これを**型（かた）**と呼びます。")

    st.write("#### 代表的な型")
    st.markdown("""
| 型の名前 | 何が入る？ | 例 |
|---|---|---|
| `int` | 整数（せいすう） | 25, -3, 100 |
| `float` | 小数（しょうすう） | 155.5, 3.14 |
| `char` | 1文字 | 'A', 'x' |
""")

    c_code2 = """#include <stdio.h>

int main(void) {
    int age = 13;            // 整数を入れる箱
    float height = 155.5;    // 小数を入れる箱
    char initial = 'A';      // 1文字を入れる箱

    printf("年齢: %d歳\\n", age);
    printf("身長: %.1fcm\\n", height);
    printf("イニシャル: %c\\n", initial);

    return 0;
}"""
    st.code(c_code2, language="c")

    st.write("実行結果：")
    st.code("年齢: 13歳\n身長: 155.5cm\nイニシャル: A", language=None)

    st.info("💡 `%d`・`%f`・`%c`は「ここに変数の中身をあてはめてね」という目印です。型によって使う記号が変わります。")

# ================= 5. 計算してみよう =================
elif menu == "5. 計算してみよう":
    st.header("5. 計算してみよう（演算子）")
    st.write("C言語では、次のような記号（演算子）を使って計算ができます。")

    st.markdown("""
| 記号 | 意味 | 例 |
|---|---|---|
| `+` | たし算 | `5 + 3` → 8 |
| `-` | ひき算 | `5 - 3` → 2 |
| `*` | かけ算 | `5 * 3` → 15 |
| `/` | わり算 | `6 / 3` → 2 |
| `%` | あまり | `7 % 3` → 1 |
""")

    c_code3 = """#include <stdio.h>

int main(void) {
    int a = 10;
    int b = 3;

    printf("たし算: %d\\n", a + b);
    printf("ひき算: %d\\n", a - b);
    printf("かけ算: %d\\n", a * b);
    printf("わり算: %d\\n", a / b);
    printf("あまり: %d\\n", a % b);

    return 0;
}"""
    st.code(c_code3, language="c")

    st.warning("⚠️ **注意**：`int`どうしのわり算は、小数点以下が切り捨てられます。`10 / 3`は`3.33`ではなく`3`になります。")

# ================= 6. もし〜なら（if文） =================
elif menu == "6. もし〜なら（if文）":
    st.header("6. もし〜なら（if文）")
    st.write("**if文**を使うと、「もし〇〇だったら、△△する」という条件による処理の分かれ道を作れます。")

    c_code4 = """#include <stdio.h>

int main(void) {
    int score = 80;

    if (score >= 60) {
        printf("合格です！\\n");
    } else {
        printf("残念、不合格です。\\n");
    }

    return 0;
}"""
    st.code(c_code4, language="c")

    st.write("実行結果：")
    st.code("合格です！", language=None)

    st.write("#### 比較に使う記号")
    st.markdown("""
| 記号 | 意味 |
|---|---|
| `==` | 等しい |
| `!=` | 等しくない |
| `>` | より大きい |
| `<` | より小さい |
| `>=` | 以上 |
| `<=` | 以下 |
""")

    st.warning("⚠️ **注意**：「等しい」を表すのは`=`ではなく`==`です。`=`は「代入」（箱に値を入れる）という別の意味なので、間違えやすいポイントです。")

# ================= 7. くり返し（for文） =================
elif menu == "7. くり返し（for文）":
    st.header("7. くり返し（for文）")
    st.write("同じ処理を何度もくり返したいときは、**for文**を使います。")
    st.write("たとえば「あいさつを5回くり返す」をプログラムで書くと、こうなります。")

    c_code5 = """#include <stdio.h>

int main(void) {
    int i;

    for (i = 1; i <= 5; i++) {
        printf("%d回目のあいさつ: こんにちは！\\n", i);
    }

    return 0;
}"""
    st.code(c_code5, language="c")

    st.write("実行結果：")
    st.code(
        "1回目のあいさつ: こんにちは！\n"
        "2回目のあいさつ: こんにちは！\n"
        "3回目のあいさつ: こんにちは！\n"
        "4回目のあいさつ: こんにちは！\n"
        "5回目のあいさつ: こんにちは！",
        language=None,
    )

    with st.expander("🔍 for文の中身を見る"):
        st.markdown("""
`for (i = 1; i <= 5; i++)` は3つの部分でできています。

- **`i = 1`**：最初にiを1にする（スタート地点）
- **`i <= 5`**：iが5以下の間はくり返す（続ける条件）
- **`i++`**：1周するごとにiを1増やす（次に進む）
""")

# ================= 8. 確認クイズ =================
elif menu == "8. 確認クイズ":
    st.header("8. 確認クイズ")
    st.write("ここまで学んだ内容をおさらいしましょう。全問正解を目指してください。")

    questions = [
        {
            "q": "Q1. C言語で画面に文字を出力する関数はどれ？",
            "options": ("選択してください", "print()", "printf()", "console.log()"),
            "answer": "printf()",
            "explain": "Pythonの`print()`に似ていますが、C言語では`printf()`を使います。",
        },
        {
            "q": "Q2. C言語のプログラムは、どの関数から実行が始まる？",
            "options": ("選択してください", "start()", "init()", "main()"),
            "answer": "main()",
            "explain": "C言語のプログラムは、必ず`main`関数からスタートします。",
        },
        {
            "q": "Q3. 整数を入れる変数の型はどれ？",
            "options": ("選択してください", "int", "float", "char"),
            "answer": "int",
            "explain": "`int`は整数（integer）を表す型です。",
        },
        {
            "q": "Q4. 「もし〜なら」という条件分岐に使う命令はどれ？",
            "options": ("選択してください", "for", "if", "printf"),
            "answer": "if",
            "explain": "条件によって処理を分けるときは`if`文を使います。",
        },
        {
            "q": "Q5. 同じ処理をくり返したいときに使う命令はどれ？",
            "options": ("選択してください", "if", "main", "for"),
            "answer": "for",
            "explain": "くり返し処理には`for`文（や`while`文）を使います。",
        },
        {
            "q": "Q6. 「等しいかどうか」を比べるときに使う記号はどれ？",
            "options": ("選択してください", "=", "==", "!="),
            "answer": "==",
            "explain": "`=`は代入、`==`は比較に使います。混同しやすいので注意しましょう。",
        },
    ]

    score = 0
    answered = 0
    for idx, item in enumerate(questions):
        st.subheader(item["q"])
        choice = st.radio("選択肢:", item["options"], key=f"quiz_{idx}")
        if choice != "選択してください":
            answered += 1
            if choice == item["answer"]:
                st.success(f"正解です！ 🎉 {item['explain']}")
                score += 1
            else:
                st.error(f"残念、不正解です。ヒント：{item['explain']}")
        st.divider()

    if answered == len(questions):
        st.write(f"### 結果：{score} / {len(questions)} 問正解")
        if score == len(questions):
            st.balloons()
            st.success("パーフェクト！🏆 C言語の基礎はばっちりです。")
        elif score >= len(questions) * 0.6:
            st.info("よくできました。もう一度見直すと、さらに理解が深まります。")
        else:
            st.warning("各項目を見直してから、もう一度チャレンジしてみましょう。")

# ================= 9. 用語集 =================
elif menu == "9. 用語集":
    st.header("9. 用語集")
    st.write("ここまでに出てきた用語をまとめました。忘れたときに見返してください。")

    terms = [
        ("変数（へんすう）", "数字や文字を入れておく、名前のついた箱のようなもの。"),
        ("型（かた）", "変数にどんな種類のデータが入るかを決めるルール（整数・小数・文字など）。"),
        ("関数（かんすう）", "決まった処理をひとまとめにしたもの。`main()`や`printf()`など。"),
        ("演算子（えんざんし）", "計算や比較に使う記号（`+`・`-`・`==`など）。"),
        ("条件分岐（じょうけんぶんき）", "「もし〜なら」で処理を枝分かれさせること。`if`文で書く。"),
        ("くり返し処理（ループ）", "同じ処理を何度もくり返すこと。`for`文などで書く。"),
        ("コンパイル", "人間が書いたプログラム（ソースコード）を、コンピュータが実行できる形式に変換すること。"),
    ]
    for term, desc in terms:
        st.markdown(f"**{term}**：{desc}")
